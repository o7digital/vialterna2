---
import DetailPage from "../../../components/DetailPage.astro";
import { platformPagesEn } from "../../../data/detailPagesEn";
export function getStaticPaths(){ return platformPagesEn.map(page=>({params:{slug:page.slug},props:{page}})); }
const { page } = Astro.props;
---
<DetailPage page={page} lang="en" />
